# 明日方舟干员系统

本文档介绍《植物大战僵尸 × 明日方舟》同人中的干员系统：架构、实现约定、动画管线、克洛丝数值与后续计划（维什戴尔等）。

## 1. 总体设计

### 1.1 干员 = PlantType 子集

干员**注册为 `CharacterRegistry.PlantType` 枚举**（P050 起，见 `scripts/autoload/global/character_registry.gd`），场景继承完整植物管线：

```
Character000Base
└── Plant000Base          (scenes/character/plant/plant_000_base.tscn)
    └── Operator000Base   (scenes/character/operator/operator_000_base.tscn)
        └── Summon000Base (scenes/character/operator/summon_000_base.tscn)
```

这样干员天然获得：卡片→手持→种植→植物格子（`PlantCell`）→被僵尸啃食→行渲染/斜面等全部能力，**与植物友方**（同一受击检测层、植物子弹不会误伤）。

- 干员类型列表：`CharacterRegistry.OperatorPlantType`，判断函数 `CharacterRegistry.is_operator_type(plant_type)`
- 干员卡片标记：`CardBase.is_operator_card`（消耗**部署点数**而非阳光）
- 可选干员白名单：`Global.global_game_state.curr_operator`（决定选卡界面干员页显示哪些）

### 1.2 部署点数（DP）

与明日方舟一致，干员消耗独立的**部署点数**，随时间自动回复，不消耗阳光：

- 管理器：`OperatorManager`（`scripts/manager/operator_manager/operator_manager.gd`，挂在主游戏 `Manager` 节点下）
- 关卡参数（`ResourceLevelData`，默认值）：
  - `is_can_use_operator = true`：本关卡是否允许干员
  - `operator_start_deploy_point = 10`：初始部署点数
  - `operator_max_deploy_point = 99`：上限
  - `operator_regen_interval = 2.0`：回复间隔（秒，≤0 不回复）
  - `operator_retreat_refund_ratio = 0.7`：撤退返还比例
- 变更广播：`EventBus.push_event("update_deploy_point", [value])`，出战卡槽（`CardSlotBattle`）订阅后更新右上角 DP 数字并刷新干员卡片可点击状态
- 测试模式（`main_game.is_test`）初始 DP 直接满额，便于调试

### 1.3 技能系统（SkillComponent）

`scripts/character/components/skill_component/component_skill.gd`，挂在干员头顶（HP 条上方），常显：

- `max_sp`：技能条上限（技能消耗）
- **SP 为浮点累计**（`curr_sp: float`）：时间回复每帧 `add_sp(delta * sp_regen_per_sec)` 小数累加，技能条**平滑增长**（不再一秒跳一格）；数字标签向下取整显示；`signal_sp_change` 首参为 float
- 回复方式 `E_SpRecoveryType`：`Attack`（攻击回复，每次攻击 +`sp_gain_per_attack`）/ `Time`（时间回复）/ `None`
- `is_auto_trigger`：就绪后是否自动触发
- 信号：`signal_skill_ready` / `signal_skill_use` / `signal_sp_change`

技能触发流程：`Operator000Base.use_skill()` → `SkillComponent.use_skill()` → 播放 `skill` 动画 → 调用虚钩子 `_on_skill_use()`（子类重写实现技能效果）。

### 1.4 血条与技能条（明日方舟风格）

干员的血条/技能条与植物僵尸不同，为头顶**迷你条**：

- 血条：15×2 px，**蓝色**填充，半透明黑色背景
- 技能条：15×2 px，**紧挨血条正下方**，亮白色填充，半透明黑色背景
- 满值时填充完全盖满背景；干员血条常显（不受控制台"显示植物血量"开关影响），不显示数字

### 1.5 干员唯一性

**每种干员场上最多部署 1 个**（明日方舟规则）：

- 卡片层：场上已有同类型干员时，干员卡片置灰不可点击（`card.gd judge_card_ready` 检查 `OperatorManager.get_operator_count_by_type`）
- 种植层：手持时虚影变透明不可种（`hm_character._update_cell_shadow` 兜底）
- 干员部署/死亡时通过 EventBus `operator_deployed` / `operator_death` 刷新卡片状态（`card_slot_battle` 订阅）

### 1.6 攻击范围与部署方向

**所有干员的攻击范围是有限的格子范围**（非单行直线），参照 Wiki 满级范围：

- 范围形状：`DetectComponentOperator.ATTACK_RANGE_SHAPE`（克洛丝速射手为 3 行 × 4 列，干员在中间行最左列）
- 部署方向：手持干员卡拖到格子上时，**方向 = 鼠标相对格子的位置**（左右/上下四向），并实时显示**攻击范围预览**（半透明白色格子）；点击种植即按当前方向部署
- 检测：`DetectComponentOperator`（继承 `DetectComponent`）每 0.1s 遍历场上僵尸，判断僵尸是否落在（按方向旋转的）范围格内，取最近者为目标，发 `signal_can_attack`
- 攻击：`AttackComponentOperator` 朝目标位置发射直线子弹（跨行 `is_activate_lane=false`）
- 干员本体：`set_attack_direction()` 设置方向并左右翻转形象

### 1.7 点击选择 → 撤退 / 技能

- 选中：`OperatorManager._unhandled_input` 在游戏阶段、**未手持卡片/铲子**时，用物理点查询（`intersect_point`，植物受击检测层 layer 2）找鼠标下的干员 → 弹出干员菜单
- 干员菜单：`scenes/operator/operator_menu.tscn`（CanvasLayer），撤退按钮在干员左上、技能按钮在干员右下（中心锚定干员中心），点菜单外空白关闭；打开时显示干员脚下攻击范围预览（挂主游戏场景根，世界坐标）
- 撤退：`Operator000Base.retreat()` → 返还 `部署费用 × operator_retreat_refund_ratio`（向下取整）→ **直接消失**（不播死亡动画；死亡才播 Spine `Die`）、释放格子、开始再部署冷却（`operator_retreat` 事件）
- 技能按钮：`use_skill()` 失败（SP 未满）时播放 buzzer 音效
- 召唤物（`Summon000Base`）默认禁用撤退与手动技能（按钮置灰）

### 1.8 数值体系基础（全干员通用，算法详见 `docs/游戏数据基础.md`，源自 [PRTS 游戏数据基础](https://prts.wiki/w/游戏数据基础)）

**星级与等级上限**（决定"满级"基准；克洛丝 ★3 → 精英1 满级 55 级，无精英2）：

| 星级 | 精英0 | 精英1 | 精英2 |
|---|---|---|---|
| ★3 | 40 | 55 | 无 |
| ★4 | 45 | 60 | 70 |
| ★5 | 50 | 70 | 80 |
| ★6 | 50 | 80 | 90 |

**属性来源**：wiki 干员页"属性"表取**满级**列（该干员可达的最高精英阶段满级——★3 取"精英1满级"列、★4~6 取"精英2满级"列；属性列不存在/干员无法精英化到该阶段时按实际可达满级列）。PVZ 侧目前只用 HP 与攻击；防御/法抗暂不参与伤害计算（可后续加物理减伤）。

**信赖加成**：信赖 100% 达上限（wiki 属性表"信赖加成上限"列）。克洛丝：攻击 +50。**当前暂未计入**（如需要按满信赖 +50 计入攻击）。

**潜能（满潜 = 潜能2~6 五档加成全部累加）**：wiki"潜能提升"表。克洛丝：潜能2 部署费用-1、潜能3 再部署-4s、潜能4 攻击+21、潜能5 天赋效果增强（10%→20%）、潜能6 部署费用-1 → **满潜合计：部署费用-2、再部署-4s、攻击+21、天赋 20%**。

**数值基准约定**：新增干员数值统一按 **"满级 + 满潜"** 填写（wiki 属性**满级**列 + 潜能五档全加；满级 = 该干员可达的最高精英阶段满级，不同星级/干员不同），信赖暂不计入。登记位置：`character_registry.gd` 的 `PlantType` 条目（SunCost=部署费用、CoolTime=再部署）+ 干员场景（HP）+ 子弹场景（攻击力）+ 干员脚本（天赋/技能钩子）。

**攻速（攻击速度）**：见 `docs/游戏数据基础.md` §3.3。实际攻击间隔 `T = T0 / (clamp(S, 10, 600) / 100)`，`S` 攻击速度默认 100，`+N 攻速` 即 `S += N`。PVZ 侧实现：`attack_component.attack_cd = T0 × 100 / S`（例如羽毛笔天赋「渐入佳境」+3 攻速/层，12 层后 `S=136`，`T0=1.3s` → 约 0.956s）。

**伤害公式**：物理/法术/元素/真实/治疗伤害公式见 `docs/游戏数据基础.md` §3.2。PVZ 侧目前只用 HP 与攻击，防御/法抗/穿透未参与伤害计算（可后续按物理伤害公式实现减伤）；「攻击力倍率」对应干员 `get_attack_paras()` 返回的 `multiplier`。

**天赋/技能机制**：天赋用概率/条件钩子（克洛丝 `get_attack_paras()` 暴击 ×1.5；羽毛笔击杀叠攻速）；技能用 `SkillComponent`（SP 上限/攻击回复/自动触发），见 §1.3。

## 2. 动画约定与 Spine 管线

### 2.1 动画命名约定（必须遵守）

干员使用 `AnimComponentOperator`（`scripts/character/components/anim_component/component_anim_operator.gd`，基于 AnimationPlayer），固定动画名：

| 动画名 | 用途 | 触发 |
|---|---|---|
| `idle` | 待机（循环） | 入场动画结束后、攻击结束 |
| `attack` | 攻击 | `AttackComponentOperator` 每轮发射后 |
| `skill` | 技能释放 | `use_skill()` 成功 |
| `die` | 死亡 | 死亡流程（淡出后移除） |

动画轨道一律作用于 `Body/BodyCorrect/OperatorSprite` 容器（`OperatorSprite` 为干员身体容器，实际美术精灵放在其下），这样无论内部精灵如何替换，动画保持生效。缺动画名时容错跳过。

- **入场动画**：完全由 Spine `Start`→`Idle` 承担（`Operator000Base.play_enter_animation()`，无 tween 淡入升起等旧占位动画）
- **死亡动画**：`character_death()` 有 Spine 形象时播 `Die`（1.0s）等 1.1s 后移除；无 Spine 时直接移除（占位动画已移除）
- **注意**：干员场景里的 `AnimationTree` 只是为满足攻击组件基类的 `$"../AnimationTree"` 引用而存在，**必须显式 `active = false`**（其默认 active 会把 BlendTree 输出持续应用，将 `OperatorSprite.modulate` 重置为 0 导致形象透明）；占位动画轨道 `update` 用 0（continuous 插值），否则动画值不插值

### 2.2 素材情况

明日方舟角色素材为 **Spine 3.8.99 骨骼动画**（`.skel` 二进制 + `.atlas` + `.png`），来自游戏客户端提取（详见根 `AGENTS.md` §10 素材管线）。项目 `bin/` 内置**自编译 spine-godot GDExtension（Spine Runtime 3.8）**，可**直接加载游戏原始 3.8.99 数据（含加密头），无需版本转换**。

克洛丝素材在 `assets/image/operator/kroos/`：`char_124_kroos.skel/.atlas/.png`（战斗模型，动画 `Idle/Attack/Start/Die/Default`）；`build_*` 为基建模型（动画 `Relax` 等，暂未用）。

### 2.3 Spine 接入（已实现，克洛丝已验证）

**形象节点**：干员场景在 `Body/BodyCorrect/OperatorSprite` 下挂 `OperatorSpineSprite`（`scripts/character/operator/operator_spine_sprite.gd`，`extends SpineSprite`），用 `@export skel_path/atlas_path` 在 `_ready` 时**运行时加载**数据（不依赖编辑器导入状态）。

**播放驱动**：`Operator000Base.get_operator_spine()` 遍历 OperatorSprite 容器找 SpineSprite；`AnimComponentOperator` 有 Spine 时驱动 Spine、无则回退 AnimationPlayer 占位动画（接口 `play_idle/play_attack/play_skill/play_die` 不变）。

动画映射：

| 逻辑动画 | Spine 动画 | 说明 |
|---|---|---|
| `idle` | `Idle` | 循环待机（2.0s） |
| `attack` | `Attack` → `Idle` | 单次攻击（0.93s），`add_animation` 自动衔接 Idle |
| 入场 | `Start` → `Idle` | `play_enter_animation()` 淡入升起后播放（0.83s） |
| `die` | `Die` | 死亡动画（1.0s） |
| `skill` | 无 | 战斗模型无技能动画，保留 AnimationPlayer 缩放提示 |

**关键约定**：`OperatorSpineSprite._ready` **只加载数据、不自动播放**——部署预览（`HM_Character` 复制 OperatorSprite 子树）需要**静态形象**；播放由 `ready_norm → play_enter_animation` 驱动。

**3.8 移植版 API 注意**（运行时实测）：`set_animation(name, loop, track)`；`add_animation(name, delay, loop, track)`（**delay 在第 2 参**）；请求不存在的动画名**静默保持当前轨道**；`get_current(track).get_animation().get_duration()` 可取时长。更多坑见 `docs/开发相关.md` "Spine 接入踩坑"。

## 3. 克洛丝（Kroos）

### 3.1 数据（按 Wiki 满级 + 满潜，可调）

| 项 | 值 | 说明 |
|---|---|---|
| 部署费用 | 8 DP | `SunCost` 字段复用（基础10 - 潜能2&6各1） |
| 再部署时间 | 66 s | `CoolTime` 字段（基础70 - 潜能3的4s） |
| HP | 1060 | 场景 `HpComponent.max_hp`（精英1满级，潜能无HP加成） |
| 攻击力 | 396 | 子弹 `bullet_101_kroos_arrow` 的 `attack_value`（基础375 + 潜能4的21） |
| 攻击间隔 | 1.0 s | `AttackComponent.attack_cd` |
| 攻击范围 | 3 行 × 4 列 | 干员在中间行最左列（速射手精英1范围） |
| 攻击方式 | 范围内朝目标发射箭矢 | 直线跨行子弹，速度 900（豌豆的 3 倍） |
| 特性 | 速射手 | `can_attack_zombie_status=9`（可攻击空中） |
| 天赋 | 要害瞄准·初级 | 满潜：20%几率攻击力×1.5（见 §3.3） |

> 注：明日方舟的防御/法术抗性在 PVZ 数值体系中没有对应，暂不参与伤害计算（可后续加物理减伤）。信赖加成（攻击+50）暂未计入。

### 3.2 箭矢子弹

`bullet_101_kroos_arrow`：**真实箭矢贴图**（`assets/image/operator/kroos/bullet/projectile_arrow.png`，114×27，箭头朝 +X；**已替换原自绘 `ArrowVisual` 光之箭**）+ **`TrailComet` 拖尾**（参数由校准 JSON 运行时应用，见下方"子弹视觉校准"）；速度 900（豌豆 300 的 3 倍），跨行直线飞行，命中后消失。

**子弹视觉校准（拖尾/子弹大小，调试工具 + 运行时应用）**：调试工具新增"拖尾长度 / 拖尾颜色 / 拖尾样式 / 子弹大小"控件——拖尾长度 = 记录点数（长度 ≈ 点数×帧间距）、颜色 = 主色（头=主色 / 中=主色×0.7 / 尾=主色×0.3 渐隐透明）、样式 = 双层亮带 / 单层亮带 / 发光(加色)（`TrailComet.E_TrailStyle`，由 `use_glow_layer`+`additive` 组合）、子弹大小 = `BulletBody.scale`（**无子弹素材的干员如维什戴尔禁用**）。「应用」写入 JSON（`trail_len`/`trail_color`[r,g,b]/`trail_style`/`bullet_scale`），游戏运行时 `OperatorCalibration.apply_to_bullet()` 在攻击组件发射子弹后调用，`TrailComet.setup()` 运行时重建亮带（无需改场景）。

**发射时机与发射点**：`bullet_spawn_delay = 0.55`（先播 Spine `Attack` 动画，延迟到动画中"松手"时刻再发射）、`bullet_spawn_offset = Vector2(80, -69)`（箭矢从弩口发出）。**校准数据存 `data/operator_calibration.json`**：`_shared` 段为全干员共用配置（容器Y/Spine位置/缩放，可调），其余键为干员 id（`operator_id`），存该干员独有的 delay+spawn+speed。游戏运行时 `OperatorCalibration` 按 `operator_id` 读取并应用（部署与部署预览都生效）。校准用调试工具 `test/operator_debug_tool.tscn`：干员下拉切换（自动扫描素材文件夹），点击画面直接设发射点，自动循环攻击，**发射该干员自己的子弹**（`OPERATOR_BULLETS` 映射，维什戴尔发射炮弹非克洛丝箭矢），**子弹速度滑块**可调，攻击动画名经 `get_anim_name` 映射（`OPERATOR_ATTACK_ANIMS` 按干员配攻击动画候选——维什戴尔 `Attack_A/B/C`，时长取最长作为延迟上限），「应用」写入 JSON。

### 3.3 天赋 要害瞄准·初级

`operator_001_kroos.gd`：攻击时 `crit_chance = 0.2`（精英1满级 20%）概率当次攻击伤害 `× crit_multiplier = 1.5`。实现于 `get_attack_paras()` 钩子（`AttackComponentOperator` 发射前调用）。

### 3.4 技能 二连射·自动（攻击回复·自动触发）

- SP 上限 5，每次攻击回复 1 点（攻击回复）；**技能发动的本次攻击（橙条倒计时期间）不计入 SP**
- SP 满后绿条保持满（不立即消耗），下次攻击开始时才消耗；也可点菜单 [技能] 手动"武装"（同样等下次攻击开始）
- 触发后**下次攻击连续射击 2 发**（每发 100% 攻击力）；**攻击动画开始时技能条变橙色满条**（`SkillComponent.start_burst_bar(攻击动画时长)` 连续橙条，非弹药格），在本次攻击动画内逐渐耗尽为 0，动画播完正好耗尽

### 3.5 部署预览

- **两段部署**：**第一段（手持/定位）**：方向固定为右，虚影所在格可种植时即显示**朝右的攻击范围预览**，且**隐藏鼠标上的干员形象**（只显示地面虚影，避免重叠；只针对干员，植物仍同时显示鼠标形象+地面虚影）；点击格子确定位置（**不创建干员**，只把预览虚影固定到目标格）进入方向选择阶段；**第二段**按鼠标朝向实时确定方向（范围预览跟随，虚影朝向跟随），点击确认才**真正创建干员并播放上场动画**（`ready_norm` → Spine `Start` → `Idle`，扣部署点）、右键取消（未创建，不扣部署点）。**预览虚影不透明度 100%**，且播放 Spine `Idle` 待机动画（非静态帧）
- **范围预览**：`OperatorRangePreview` 按**真实草坪格子节点**生成范围（与草坪完全对齐，越界格跳过；范围含干员所在格——`ATTACK_RANGE_SHAPE` 含 `(0,0)` 即显示，特殊干员形状中不含则不显示），每格矩形边长 = **网格间距 + 3px 重叠**（相邻格子间若有空隙，各向对方延伸半个空隙 = 无缝覆盖；重叠消除 Polygon2D 光栅化细缝，右/上方向都有）；**角相邻（斜对角）的格子对在共享角点插入直角桥接方块**（`BRIDGE=6`），缺口补上且轮廓全部为 90° 直角（无 45° 斜边、无圆角、**无边框**）；条纹为 **shader 45° 橙/透明斜纹**（`shaders/operator_range_stripes.gdshader`，参考 spatial 版算法：世界坐标投影到条纹方向 + `mod` + `count=5` 条累加 + `smoothstep` 软边；`width=16.5`、`spacing=165` = `count × 2 × width`，**条纹宽与间隔等距**；`base_color` 淡橙底 alpha 0.10 盖住草坪网格线）白色小箭头与提示文字。**2026-08 起部署预览与干员菜单预览统一挂主游戏场景根（世界画布，identity 变换，z_index=30）**——条纹在世界坐标空间计算、随分辨率与草坪同步缩放，任何窗口尺寸下条纹与格子的相对宽度一致；shader 掩码必须写 `1.0 - smoothstep(width - 0.25, width, p)`（`smoothstep(width, width-0.25, p)` 是 GLSL 未定义行为）
- **转身/正背面**（`operator_000_base.gd` + `DetectComponentOperator` 驱动）：干员默认正面素材（BattleFront），`OperatorSpineSprite` 同时加载背面素材（`back_skel_path/back_atlas_path`，BattleBack，方舟战斗中干员背对镜头视角）；**只有朝上（Up）部署时默认背面**（`is_default_back_direction`），入场显示背面且**素材一直保持背面**（`reset_visual` 恢复到默认素材，朝上部署入场后不攻击也不切正面）；**有攻击目标时**：目标在干员**上方行**（`lane < 干员行`）→ `set_back_visual(true)` 切背面素材 + `scale.x` 取反（转身），目标在下方行/同排 → 正面素材（背面反之）；**目标在反向列**（干员背后方向但仍需在攻击范围内，`behind_turn_cols=0.4` 格）→ `set_behind_visual(true)` 只 `scale.x` 反转不切换素材；无目标 → `reset_visual()` 恢复默认。转身带**动画**（scale.x→0 变薄 → 归零切素材 → 恢复目标 scale，`TURN_ANIM_TIME`）。**预览虚影**按方向切素材（方向选择阶段鼠标移到朝上=后时虚影也切背面素材，`hm_character._update_shadow_back_visual`）。素材切换保持当前动画（`switch_data` 重播缓存动画名；正/背数据懒加载支持复制节点）
- **干员菜单攻击范围小图标**：`OperatorMenuRangeIcon` 网格图，点亮范围内格（橙黄），干员格白点标记
- 手持/虚影：**Spine 战斗形象静态帧**（干员场景 `Body/BodyCorrect/OperatorSprite` 复制，含 `OperatorSpineSprite`；只加载数据不播放动画），与部署后一致、大小合适（非卡片立绘）
- **再部署 CD**：部署时不开始冷却，撤退/死亡后才按卡片 `cool_time` 开始计（取消部署不触发冷却）

### 3.6 相关文件

- 脚本：`scripts/character/operator/operator_001_kroos.gd`、`scripts/character/operator/operator_spine_sprite.gd`（Spine 形象节点）、`scripts/character/operator/operator_calibration.gd`（校准 JSON 读取/应用）
- 场景：`scenes/character/operator/operator_001_kroos.tscn`（`KroosSpine` 为 SpineSprite，加载 `char_124_kroos.skel/.atlas`）
- 校准数据：`data/operator_calibration.json`（`_shared` 段=全干员共用配置，其余键=干员 id 存 delay+spawn，由调试工具写入）
- 子弹：`scenes/bullet/bullet_101_kroos_arrow.tscn`（贴图 `projectile_arrow.png` 114×27 + `TrailComet` 拖尾）
- 音效：`assets/audio/SFX/operator/kroos_arrow_shot.wav`（发射，方舟 `p_atk_arrow_n`）+ `kroos_arrow_hit.wav`（命中，`p_atk_arrow_h`），从游戏 `audio/sound_beta_2/player/p_atk_0.ab` 用 ArkUnpacker `--audio` 提取；SoundManager 键 `KroosArrowShot`/`KroosArrowHit`（子弹 `_ready`/`attack_once` 播放）
- 卡片：`scenes/autoload/all_cards.tscn` → `PlantCards2/Card5`
- 验证：`test/spine_check.gd`（数据加载冒烟）、`test/operator_spine_check.gd`（干员场景动画流转冒烟）、`test/spine_demo.tscn`（可视化演示）、`test/operator_debug_tool.tscn`（干员调试工具：攻击时序/发射点/格内位置校准，**新增干员必用**；「应用」按钮直接写入场景文件）

## 4. 维什戴尔（Wisadel，投掷手，★6 限定）——已实现

### 4.1 数据（满级精英2 90 + 满潜 + 信赖加成）

| 项 | 值 | 说明 |
|---|---|---|
| 部署费用 | 23 DP | `SunCost`（基础25 - 潜能2&6各1） |
| 再部署时间 | 70 s | `CoolTime`（基础70；满潜-4=66s，按需求取70） |
| HP | 1888 | 场景 `HpComponent.max_hp`（精英2满级） |
| 攻击力 | 809 | 炮弹 `bullet_102_wisdel_shell.attack_value`（687满级 + 32潜能 + 90信赖） |
| 攻击间隔 | 2.1 s | `AttackComponent.attack_cd` |
| 攻击范围 | 5 行 × 5 列大范围菱形 | `WISADEL_RANGE_SHAPE`（干员在中间行最左列） |
| 特性 | 投掷手 | 攻击对小范围地面敌人两次物理伤害（第二次余震=攻击力一半），溅射半径1.1格 |
| 天赋1 好礼 | 常驻 | 主目标伤害×1.25+附残影；带残影目标被余震影响时15%爆炸（175%攻击物理伤害+眩晕1s） |
| 天赋2 死魂灵的余息 | 常驻 | 部署动画结束后在攻击范围内召唤1个魂灵之影（初始0技力） |

### 4.2 三个技能（选卡时选择，见 §4.4 技能选择界面）

| 技能 | 类型 | SP | 效果（专三） |
|---|---|---|---|
| 1 定点清算 | 攻击回复·自动触发 | 0/2 | 下次攻击额外2次余震并使所有目标晕眩1.5秒，溅射范围略微扩大(1.5格)，余震伤害变为攻击力的120%（×3次） |
| 2 饱和复仇 | 自动回复·手动触发·可随时关闭 | 15/25，持续25s | 攻击+35%、攻击间隔缩短(-0.7)→1.4s、同时攻击3名敌人；开启13s后进入过载：80%攻击力4连发随机攻击范围内目标 |
| 3 爆裂黎明 | 自动回复·手动触发 | 40/50 | 召唤2个魂灵之影（最多3个，技能结束后保留；初始技力3sp优先/0sp）；攻击力+180%、攻击间隔+2.9→5.0s、攻击时攻击力220%、溅射大幅扩大(2.5格)、天赋爆炸概率100%、6发弹药打完结束（可随时停止）；期间可对空 |

**攻击表现**：每次攻击发射直线炮弹（`bullet_102_wisdel_shell`，速度620），命中主目标直击（×天赋1.25）+ 溅射范围内所有敌人受余震（×0.5，技能1时×1.2×3）+ 残影爆炸判定；攻击动画在 `Attack_A/B/C` 三选一随机播放（素材有三种攻击方式）。

**爆炸特效**：~~已从项目分离~~（原 20 层 GPUParticles3D 特效 + 解析生成器 + 全部特效贴图已备份到 `E:/arkext_backup/wisdel_fx/`，含 `scenes_fx/scripts_fx/textures_shared/textures_wisdel/test/tools_fxdep` 六目录）。当前命中/技能爆发**无特效**，炮弹为彗星式弹头贴图（`assets/image/operator/wisdel/bullet/wisdel_03.png`，仿方舟游戏内炮弹出膛效果：亮红橙发光弹头朝 +X 方向 + 暗红近黑烟雾尾迹，由 `tools/gen_wisdel_shell.py` 纯标准库程序化生成，改参数后重跑并强制重导入即可）。若后续要恢复，从备份目录拷回并重新接入 `bullet_102_wisdel_shell` 命中与三技能释放两处调用点即可。

**炮弹拖尾**（2026-08 增强，参考 [godotshaders.com "Particle based trail"](https://godotshaders.com/shader/particle-based-trail/)，3D GPUParticles 拉伸条带拖尾思路的 2D 等价实现）：
- **拖尾特效代替炮弹本体**：炮弹**不再有贴图**（`BulletBody` 覆盖已移除），飞行视觉全部由拖尾承担——炮弹即一道红色彗星亮带（`wisdel_03.png` 与 `tools/gen_wisdel_shell.py` 保留但已无引用，可删）
- **动态双层亮带**（`TrailComet`，`scripts/bullet/component/trail_comet.gd`，通用可复用）：每物理帧记录父节点飞行轨迹，用**外层柔光 + 内层亮芯**两条 Line2D 绘制；**暗红色**（头部 `(0.7, 0.08, 0.12)` → 中部暗红 → 尾部更暗红透明）；**默认普通混合**（`additive=false`，暗色调在 PVZ 亮绿草地上用加色混合压不暗，必须普通混合直接呈现；需要发光感再开 `additive=true`）；场景配 `max_points=64`、`head_width=15`（柔光约 39px），速度 620 下亮带长约 600px+；`head_extend=0`（无炮弹可接，亮带头部即子弹当前位置）
- 命中销毁前炮弹脚本调 `detach_and_fade()` 让亮带**留在原地淡出**（0.16s）而非瞬间消失
- 冒烟测试：`test/trail_comet_check.gd`（`--script` 模式直接验证组件，零 autoload 依赖；炮弹场景本身因引用 `SoundManager` 无法在 --script 模式编译，需编辑器编译验证）

**技能2 过载时机**：开启后 13 秒进入过载（`SKILL2_OVERLOAD_AFTER`，可调）；过载模式下不再多目标，改为 4 连发随机索敌（与 PRTS 备注一致）。

### 4.3 魂灵之影（召唤物，Shadow of Revenant）

- 场景 `scenes/character/operator/summon_002_wisdel_shadow.tscn`，脚本 `extends Summon000Base`
- 数值（精英2满级）：HP 3500 / 攻击 777 / 部署费用 0 / 再部署 5s / 攻击间隔 1.0s / 阻挡 0
- **不进行普通攻击**：技能（自动回复·自动触发，SP5，时间回复1点/秒）即攻击——对维什戴尔攻击范围内 1 名敌人造成 100% 攻击力法伤 + 1秒停顿（`be_butter`）+ 附着残影 + 之后获得0-2点技力
- **技能满后攒着等目标**：SP 满后若范围内无敌人，技能不释放、SP 不消耗（`SkillComponent.auto_trigger_check` 自动触发前检查，见 §4.6），等有敌人进入范围才释放（修复原"没敌人也白白放掉重计"的问题）
- 目标选取：**优先攻击未携带残影的敌人**（PRTS 备注），否则最近敌人；范围/方向继承主人维什戴尔（`get_attack_range_shape` 返回主人形状）
- 部署位置：攻击范围内空格，规则"距离自身最近 > 下方行 > 左侧列"（`_find_shadow_cell` 简化实现）
- 上限 3 个；维什戴尔撤退/死亡时全部消失（`_remove_all_shadows`）
- 迷彩/禁疗：**暂未实现**（PVZ 无对应机制）
- **不转身**：召唤物保持部署时朝向（`update_visual_for_target` 空实现），无背面素材
- **Spine 素材**：`assets/image/operator/wisdel/shadow/wward.*`——从游戏 `skinpack/token_10035_wisdel_wward.ab`（魂灵之影素材 id = `wisdel_wward`）用 ArkUnpacker 提取，动画 `Idle/Attack/Die/Start`（无背面，技能触发播 `Attack`→`Idle`）
- **攻击射线特效**：`scenes/fx/wisdel_shadow_beam.tscn`（脚本 `scripts/fx/wisdel_shadow_beam.gd`，`SceneRegistry.WISDEL_SHADOW_BEAM`）——仿游戏内全过程：**宽约10px的等宽红色线段**从魂灵之影向僵尸连线伸展（0.09s）→ 线段到达僵尸即命中（`signal_beam_hit` 一次性回调 `_on_beam_hit`：法伤+停顿+附着残影，**伤害结算在命中帧**，不再瞬发）→ 命中点白红四芒星闪光 + 暗红碎片爆发+蓝色小电火花，**射线宽度逐渐缩小直至消失**，全程约 0.8s 自行销毁。贴图 `assets/image/operator/wisdel/beam/{beam,star,burst,spark}.png` 由 `tools/gen_wisdel_shadow_beam.py` 程序化生成；z 层级按目标行 `lane*50+40`（同火爆辣椒火焰约定）；冒烟测试 `test/wisdel_shadow_beam_check.gd`

### 4.4 技能选择界面（仿模仿者，新增通用功能）

**需求**：选卡阶段点击维什戴尔卡片 → 弹出技能选择面板（1/2/3技能）→ 选定后卡片放入出战卡槽并携带所选技能；未选技能时点击卡片只弹面板不放入。

- **卡片字段**（`card_base.gd`）：`operator_skill_id`（所选技能，默认1）、`is_multi_skill_operator`（是否弹技能面板，维什戴尔卡 true）、`is_skill_choosed`（选卡时是否已选）
- **面板（数据驱动，2026-08 重构）**：`CardSlotCandidate/AllOperatorSkillCard`（`scenes/card_slot/card_slot_norm.tscn`）——全屏覆盖 Control（mouse_filter IGNORE）+ 居中 `Panel`(470×480，锚点 0.5/0.5) + `SkillList`(VBox，**内缩 36px**，内腔 398×408，避开面板 ~30px 装饰边框；标题 22 号字、按钮 15 号字、取消按钮 40 高) + `SkillButtonList`（按钮由代码按注册表动态生成）+ 取消按钮。**技能数据登记在注册表 `PlantInfoAttribute.OperatorSkills`**（`{技能id: {name, desc, icon}}`），`operator_skill_card_appear(干员类型)` 动态建按钮——新多技能干员只登记数据不改面板。**注意 Godot 4.7 Container 最小尺寸钳制**（尺寸会被钳制为不小于内容最小尺寸）：按钮不自动换行，desc 每行必须 ≤ 约20个汉字、字号 15，否则按钮最小宽把 VBox 撑出 Panel（"UI 超出页面框"根因，见 `docs/开发相关.md` 坑 27）；**面板节点必须 `visible = false`（且 `_ready` 防御性隐藏）**——编辑器自动保存会省略默认值行，丢掉后开局显示空技能页
- **流程**（`card_slot_norm.gd`）：`_on_card_click` 拦截多技能干员（未选技能）→ 弹面板 → `_on_skill_choosed(skill_id)` 写卡并 `_do_card_click` 放入出战槽；取消按钮 → `_on_operator_skill_card_close` 不放入；再点已选卡取消选择时重置 `is_skill_choosed`
- **存档**：选卡保存 `selected_cards` 时写入 `operator_skill_id`，重选卡（`_on_re_card_button_pressed`）恢复
- **出战**：`hm_character.confirm_operator_direction` 创建干员后调用 `apply_operator_skill(技能id)`（干员 `ready_norm` 已按默认一技能配置，创建后重配）

### 4.5 相关文件

- 脚本：`scripts/character/operator/operator_002_wisdel.gd`（技能/天赋/召唤）、`scripts/bullet/bullet_102_wisdel_shell.gd`（炮弹+溅射+残影爆炸）、`scripts/character/operator/summon_002_wisdel_shadow.gd`
- 场景：`scenes/character/operator/operator_002_wisdel.tscn`、`summon_002_wisdel_shadow.tscn`、`scenes/bullet/bullet_102_wisdel_shell.tscn`
- 素材：`assets/image/operator/wisdel/`（正面）+ `back/`（背面），**ArkUnpacker 提取**（`BattleFront/BattleBack` 两套；`extract_ab.py` 同名覆盖只留正面）
- 技能图标：`assets/image/operator/wisdel/skill_icon_1/2/3.png`——从游戏 `spritepack/skill_icons_*.ab` 提取（`skill_icon_skchr_wisdel_1/2/3`），用于技能选择面板按钮与干员菜单技能按钮（`Operator000Base.get_skill_icon()` 按所选技能返回；默认克洛丝图标兼容）
- 炮弹贴图：~~`assets/image/operator/wisdel/bullet/wisdel_03.png`~~——维什戴尔包内**无独立炮弹贴图**（投掷物由武器动画/特效表现）；现炮弹飞行视觉**由拖尾亮带代替**（`TrailComet`，见 §4.2 炮弹拖尾），弹头贴图与 `tools/gen_wisdel_shell.py` 已无引用（保留备份，可删）；`assets/image/operator/wisdel/shell.png`（魂灵之影图集 `C_AFunnel_A` 浮游炮裁出）已无任何引用，属遗留素材
- 音效：`assets/audio/SFX/operator/wisdel_attack.wav`（炮击发射，方舟 `p_atk_blackcannon_s`）+ `wisdel_explode.wav`（爆炸，`p_skill_aeroliteexplo`），从游戏 `audio/sound_beta_2/player/p_atk_0.ab`/`p_skill_0.ab` 用 ArkUnpacker `--audio` 提取；SoundManager 键 `WisdelAttack`/`WisdelExplode`（炮弹 `_ready`/`attack_once` 播放）
- 技能动画：维什戴尔战斗模型动画 `Skill_1`（技能1一次性）、`Skill_2_Begin/Idle/End`、`Skill_2_Overload_Begin/Idle`（过载）、`Skill_3_Begin/Idle/End`（三技能）；`_on_skill_use`/过载切换/技能结束分别播放对应动画
- 卡片：`scenes/autoload/all_cards.tscn` → `PlantCards2/Card6`（`is_multi_skill_operator = true`）
- 动画名：正面/背面均为标准名 `Idle/Attack_A/Attack_B/Attack_C/Start/Die/Skill_2_Begin/Skill_2_Idle/Skill_2_End/Skill_3_Begin...`（**无 Attack 单动画**，三种攻击随机；skel 内 `Attack_Av` 等带 v 的是变形目标名，勿用作动画名）
- 验证：`test/wisdel_spine_check.gd`（Spine 加载+动画冒烟）、`test/wisdel_operator_check.gd`（干员场景/技能配置冒烟，需编辑器运行——`--script` 模式无 autoload）

### 4.6 组件扩展（维什戴尔引入的通用改动）

- `DetectComponentOperator`：`ATTACK_RANGE_SHAPE` 保持默认（克洛丝3×4），static 方法加 `shape` 可选参数；`_get_lane_offsets` 改为按形状动态计算（支持 5 行范围）
- `Operator000Base`：新增 `get_attack_range_shape()`（子类覆盖）；`use_skill()` 改为依赖 `signal_skill_use` 统一触发（修复组件自动触发不调 `_on_skill_use` 的问题）
- `AttackComponentOperator`：`get_attack_paras` 支持 `targets`（多目标/随机目标）与 `splash_paras`（子弹定制参数）；**攻击周期重构（2026-08）**：one-shot 计时器 + 每轮手动武装，索敌后立即攻击（无随机初始延迟）、连射带 `burst_interval` 间隔（二连发可见）、下一轮间隔 = `max(攻击间隔, 攻击动画时长)`（攻击动画播完才开始下一轮）、计时器到点目标过期立即重新索敌（不整轮跳过）——详见 `docs/开发相关.md` 坑 26
- `SkillComponent`：新增 `initial_sp`（方舟"初始技力"）、`is_skill_active`/`signal_skill_ended`/`end_skill()`（持续技能可提前关闭；激活期间不回复SP）；新增 **`auto_trigger_check`**（自动触发前条件 Callable，返回 true 才触发；未设置视为恒真。技能就绪后由 `_process` 每帧重试该条件——用于"技能满后攒着，等目标进入范围再释放"，魂灵之影使用，见 §4.3）
- `OperatorSpineSprite`：动画名可配置（`anim_name_idle/enter/die` + `attack_anim_names` 多攻击随机）；`AnimComponentOperator` 播放改用映射
- `OperatorCalibration`：新增 `apply_to_bullet(bullet, operator_id)`——发射时把子弹视觉校准（拖尾长度/颜色/样式、子弹大小）应用到子弹（`component_attack_operator` 发射后调用）；JSON 键 `trail_len`/`trail_color`/`trail_style`/`bullet_scale`
- `TrailComet`（`scripts/bullet/component/trail_comet.gd`）：新增 `E_TrailStyle`（双层/单层/发光）、`use_glow_layer`、`setup()` 运行时按校准参数重建亮带（长度/主色/样式）；所有干员子弹（克洛丝箭矢/维什戴尔炮弹）都挂该组件
- 天赋残影：挂在僵尸 `meta("wisdel_residue")`，退场自动消失（不修改僵尸基类）

## 5. 桃金娘（Myrtle，先锋·执旗手，★4）

### 5.1 数据（满级精英2 70 + 满潜）

| 项 | 值 | 说明 |
|---|---|---|
| 部署费用 | 8 DP | `SunCost`（基础10 - 潜能2&6各1） |
| 再部署时间 | 66 s | `CoolTime`（基础70 - 潜能3的4s） |
| HP | 1565 | 场景 `HpComponent.max_hp`（精英2满级） |
| 攻击力 | 520 | 子弹 `bullet_104_myrtle_hit.attack_value`（精英2满级） |
| 攻击间隔 | 1.3 s | `AttackComponent.attack_cd` |
| 攻击范围 | 1-1（自身所在格） | 近战，`get_attack_range_shape() = [Vector2i(0,0)]` |
| 特性 | 技能期间阻挡数变为0 | PVZ 无阻挡机制，暂未实现 |
| 天赋 浮光跃金 | 满潜 28 点/秒自身回血 | 原效果为"所有先锋干员回血"，当前仅桃金娘为先锋，简化为自身 |

### 5.2 两个技能（选卡时选择，均自动回复·手动触发·持续技能）

| 技能 | SP | 效果（专三） |
|---|---|---|
| 1 支援号令·β型 | 初始13/消耗22，持续8s | 停止攻击，8秒内回复总共14点部署费用 |
| 2 治愈之翼 | 初始10/消耗24，持续16s | 停止攻击，16秒内回复总共16点部署费用，并每秒治疗一名友方50%攻击力 |

技能期间通过 `attack_component.disable_component(E_IsEnableFactor.Character)` 停止攻击，技能结束恢复；DP 回复按 `_skill_dp_per_sec` 逐帧累计，治疗按 1s 节拍 `_heal_lowest_ally` 治疗生命比例最低的友方（不含满血）。

### 5.3 Spine 动画

- 动画名与克洛丝/维什戴尔一致：`Idle/Attack/Start/Die/Skill_Begin/Skill_Loop/Skill_End`（**无尾下划线**；`Default` 为 0s 静态姿势，不用）
- **坑**：`char_151_myrtle.skel` 中可见 `Idle_`/`Attack_` 等带尾下划线的字符串，那是骨骼/附件名，**不是动画名**，勿用作动画名
- **坑**：背面素材（`back/`）**缺少 `Die` 动画**，背面朝向死亡时 `play_spine` 会回退 idle（等待时长略长，可接受）
- 素材 `assets/image/operator/myrtle/`（正）+ `back/`（背），ArkUnpacker 提取后 `merge_alpha.py` 合成 alpha（约 59% 透明）

### 5.4 相关文件

- 脚本：`scripts/character/operator/operator_003_myrtle.gd`、`scripts/bullet/bullet_104_myrtle_hit.gd`
- 场景：`scenes/character/operator/operator_003_myrtle.tscn`、`scenes/bullet/bullet_104_myrtle_hit.tscn`
- 素材：`assets/image/operator/myrtle/`（`char_151_myrtle.skel/.atlas/.png` + `back/` + `myrtle_card_full.png` + `skill_icon_1/2.png`）
- 种植条件：`resources/character_resource/plant_condition/052_myrtle_operator.tres`
- 卡片：`scenes/autoload/all_cards.tscn` → `PlantCards2/Card7`（`is_multi_skill_operator = true`）
- 校准初始值：`test/operator_debug_tool.gd` → `OPERATOR_GAME_VALUES["myrtle"]`
- 冒烟测试：`test/myrtle_spine_check.gd`（`--script` 模式验证正/背面 Spine 加载与动画播放）

## 6. 羽毛笔（La Pluma，近卫·收割者，★5）

### 6.1 数据（满级精英2 80 + 满潜）

| 项 | 值 | 说明 |
|---|---|---|
| 部署费用 | 20 DP | `SunCost`（基础22 - 潜能2&6各1） |
| 再部署时间 | 60 s | `CoolTime`（基础70 - 潜能3的4s - 潜能5的6s） |
| HP | 2250 | 场景 `HpComponent.max_hp`（精英2满级） |
| 攻击力 | 650 | 子弹 `bullet_105_crow_slash.attack_value`（精英2满级，信赖+75暂不计入） |
| 攻击间隔 | 1.3 s | `AttackComponent.attack_cd`（受天赋/技能动态缩短） |
| 阻挡数 | 2 | 场景 `block_count`（精英2） |
| 攻击范围 | 自身格 + 前方一列 3 格 | `get_attack_range_shape() = [(-1,1),(0,0),(0,1),(1,1)]` |
| 特性 | 群体伤害 + 命中回血 + 无法被友方治疗 | 每命中一个敌人回复50生命，最大生效数=阻挡数(2) |
| 天赋 渐入佳境 | 每击杀 +3 攻速，最多 12 层 | 攻速系数 100/(100+层数*3)，动态缩短攻击间隔 |

### 6.2 两个技能（选卡时选择）

| 技能 | 类型 | SP | 效果（专三） |
|---|---|---|---|
| 1 高速切割 | 攻击回复·自动触发 | 0/2 | 下次攻击攻击力提升至165%，连续攻击两次 |
| 2 收割 | 自动回复·手动触发·持续 | 30/40，持续25s | 攻击力+70%，攻击间隔-50%，对生命值<50%敌人额外+50%伤害 |

### 6.3 实现要点

- 群体伤害：`get_attack_paras()` 返回 `count=1`（一次挥砍只发射一发 `bullet_105_crow_slash`），命中后由 `on_crow_slash_hit()` 结算攻击范围内所有敌人（低血增伤/回血/击杀上报在干员侧）。
- 技能1 二连击（近卫多连击）：`get_attack_paras()` 返回 `hit_times=2`，子弹命中时 `on_crow_slash_hit(hit_times)` 对范围内每个敌人一次性施加两次攻击（动画不变，特效暂未做）；**技能条在下次攻击动画开始时变橙色满条**（`SkillComponent.start_burst_bar(攻击动画时长)` 连续橙条，非弹药格），在本次攻击动画内逐渐耗尽为 0；**技能发动的本次攻击不计入 SP**。
- 命中回血：`on_crow_slash_hit()` 按 `min(命中敌人数, block_count) × 50` 每次连击独立结算（技能1二连击 = 两次命中各触发一次）。
- 低血增伤：由子弹 `bullet_105_crow_slash._attack_zombie()` 按目标当前血量比例判定（技能2激活时 `low_hp_bonus=0.5`），在已乘攻击倍率后再乘。
- 天赋攻速：子弹命中致死时回调 `on_crow_kill()`，叠加层数并 `_update_attack_cd()` 重算 `attack_component.attack_cd`，**同时同步加快 Spine 攻击动画**（`OperatorSpineSprite.set_anim_time_scale(1/间隔系数)`）——否则 `_next_interval()` 的 `max(攻击间隔, 动画原始时长)` 会被动画时长卡住，攻速看起来没生效。
- 无法被友方治疗：`can_be_healed_by_ally()` 返回 false（`Operator000Base` 新增钩子），桃金娘二技能 `_heal_lowest_ally()` 跳过。

### 6.4 相关文件

- 脚本：`scripts/character/operator/operator_004_crow.gd`、`scripts/bullet/bullet_105_crow_slash.gd`
- 场景：`scenes/character/operator/operator_004_crow.tscn`、`scenes/bullet/bullet_105_crow_slash.tscn`
- 素材：`assets/image/operator/crow/`（`char_421_crow.skel/.atlas/.png` 正面 + `back/` 背面 + `crow_card_full.png` + `skill_icon_1/2.png`）
- 种植条件：`resources/character_resource/plant_condition/053_crow_operator.tres`（plant_condition=54）
- 卡片：`scenes/autoload/all_cards.tscn` → `PlantCards2/Card8`（`is_multi_skill_operator = true`）
- 校准初始值：`test/operator_debug_tool.gd` → `OPERATOR_GAME_VALUES["crow"]`

> 素材说明：战斗 Spine 用 ArkUnpacker `-m ab --spine --image --text` 提取 `char_421_crow.ab` 得 `BattleFront`/`BattleBack` 两套，alpha 用 `merge_alpha.py` 合成；动画名 `Idle/Attack/Start/Die`（背面缺 `Die`，同桃金娘坑）。

## 7. 新增干员步骤（速查）

1. **提取 Spine 素材（必做第 1 步，每次添加干员都必须先做，禁止用占位立绘代替）**：ArkUnpacker `tools/ArkUnpacker-v5.1.0.exe -m ab --spine --image --text` 解 `chararts/char_<id>.ab` → `BattleFront`（正）/`BattleBack`（背）两套三件套；`python tools/spine_extract/merge_alpha.py <主>.png <alpha>.png` 合成 alpha；正面放 `assets/image/operator/<id>/`、背面放 `<id>/back/`，卡牌整卡图与技能图标也放该目录。改素材后**强制重导入**（删 `.import` + `.godot/imported/` 缓存，headless 跑两次）。
2. `CharacterRegistry.PlantType` 加枚举（P053 起），`OperatorPlantType` 加入类型
3. `PlantInfo` 登记：`PlantName / CoolTime(再部署) / SunCost(部署费用) / PlantConditionResource / PlantScenes` + **数据驱动字段**：`OperatorDisplayName`（中文显示名）、`OperatorCardBg`（整卡图）、`OperatorBulletScene`（攻击子弹场景）、`OperatorAttackAnims`（攻击动画候选）、`OperatorAttackModes`（调试工具模拟技能用）；**多技能干员另登记 `OperatorSkills`**
4. 从 `operator_000_base.tscn` 新建继承场景 + 脚本 `extends Operator000Base`，设 `plant_type`、`operator_id`；`Body/BodyCorrect/OperatorSprite` 下挂 `OperatorSpineSprite`（`skel_path`/`atlas_path` + `back_skel_path`/`back_atlas_path` + 动画名），并隐藏 `PlaceholderSprite`
5. 攻击/检测：`AttackComponentOperator`（`attack_cd`/`attack_bullet_type`/`bullet_spawn_offset`/`bullet_spawn_delay`）+ `DetectComponentOperator`（`can_attack_zombie_status`）；**范围不同则覆盖 `get_attack_range_shape()`**（默认克洛丝 3×4）
6. 重写 `get_attack_paras()`（连射/倍率/多目标）与 `_on_skill_use()`（技能效果）
7. 校准：运行 `test/operator_debug_tool.tscn` → 下拉选新干员 → 点画面设发射点 → 拖延迟/发射点 → **「应用」**写 `data/operator_calibration.json`（运行时读取覆盖场景默认值）；在工具脚本 `OPERATOR_GAME_VALUES` 登记校准初始值（JSON 无此干员时回退）
8. 卡片与白名单：`scenes/autoload/all_cards.tscn` 的 `PlantCards2` 加 `Card`（`card_plant_type` + `is_operator_card=true`；多技能干员加 `is_multi_skill_operator=true`）；`Global.global_game_state.curr_operator` 加白名单
9. 音效（可选）：默认部署/死亡/技能发动音效由基类 `get_deploy_sfx()`/`get_death_sfx()`/`get_skill_use_sfx()` 钩子提供；有专属音效时覆盖钩子返回专属键

## 8. 已知取舍（欢迎调整）

- 撤退**不重置**卡片冷却（与铲子一致）；完整的"再部署时间重置"留待后续
- 部署点数数值（初始 10 / 上限 99 / 2s 回复 1 点）为初版平衡，可在关卡参数或注册表中调整
- 攻击范围形状已做成**每干员配置**（`Operator000Base.get_attack_range_shape()`，默认克洛丝 3×4；维什戴尔 5×5 菱形）
- 部署方向为 4 向，通过"鼠标相对格子的位置"选择；上/下方向在 PVZ 横向战场中实际作用有限
- 干员存档目前跟随植物存档流程（`get_save_game_data_plant`），技能点/部署点数不进存档；**所选技能**（`operator_skill_id`）随选卡 `selected_cards` 保存
- 维什戴尔：残影爆炸/余震伤害为简化数值（余震×3 合并、爆炸不乘技能倍率）；魂灵之影迷彩/禁疗未实现；二技能过载时机（13s）为自定义值；三技能"爆裂锁定"未实现（直接攻击范围内目标）；炮弹贴图与魂灵之影射线特效已为程序化生成的仿原作效果（`tools/gen_wisdel_shell.py` / `tools/gen_wisdel_shadow_beam.py`），命中/技能爆发大特效仍在备份 `E:/arkext_backup/wisdel_fx/` 未恢复
